apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: angi-helloworld
  namespace: default
spec:
  interval: 5m
  url: https://github.com/lambrech-hsrt/flux-infra-basic
  ref:
    branch: main
  ignore: |-
    # exclude all
    /*
    # include charts directory
    !/charts/
